package org.marshal;

public class PracticeTest {

    //скролл по вертикали на 3500px от текущего положения
    //js.executeScript("window.scrollBy(0, 3500)");

    //скролл до поиска указанного элемента
    //js.executeScript("arguments[0].scrollIntoView(true);",itemListSiderBar);
}
